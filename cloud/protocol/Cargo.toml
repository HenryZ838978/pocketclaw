[package]
name = "crab-protocol"
description = "CrabAgent phone↔cloud communication protocol types"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }
